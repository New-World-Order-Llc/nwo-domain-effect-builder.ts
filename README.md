# nwo-domain-effect-builder.ts
Builds deterministic domain‑effect packets from validated execution results. Applies contract‑defined domain rules, ensures reproducible effect generation, and produces immutable packets for the transition engine. Prevents nondeterministic domain behavior and enforces strict separation between effects and state mutation.
